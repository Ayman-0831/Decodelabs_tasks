# Task-3: SQL Data Analysis

This task focuses on data querying and analysis using SQL functions.

### Key Work Done:
- Used SELECT statement to retrieve data
- Applied DISTINCT to remove duplicate records
- Used WHERE clause for filtering data
- Applied GROUP BY for grouping data
- Used ORDER BY for sorting results
- Used Aggregate Functions: COUNT, AVERAGE (AVG), SUM

### Tools Used:
MS Access.
- SQL Queries: SELECT, DISTINCT, WHERE, GROUP BY, ORDER BY, COUNT, AVG
